package com.emirerkut.movieapp.navigation

enum class Destination(
    val route: String,
) {
    HOME(
        route = "home",
    ),
    SEARCH(
        route = "search",
    ),
    DETAIL(
        route = "detail",
    )
}